● Doc Alignment: 10% Shift to Reality

  What Docs Say Now (Confusing)

  Layer 4 (Retrieval):
  def explain(query):
      # Bundle context
      return {...}

  def trace(query):
      # Genealogy timeline
      return {...}

  Implies: Build wrapper functions before testing.

  ---
  What Actually Happens (Real Usage)

  # Compose flags at runtime
  imem develop search "auth" --decisions --failures --limit 10

  # Claude Code composes
  # No wrappers needed

  Reality: CLI primitives + Claude intelligence = sufficient.

  ---
  Minimal Doc Edits

  architecture.md - Layer 4 section:

  OLD:
  ### Mode 2: EXPLAIN (Context Retrieval) ❌ PLANNED
  def explain(query, collection_name):
      ...

  NEW:
  ### Mode 2: EXPLAIN (Context Retrieval) ❌ FUTURE
  **Composed at runtime via CLI:**
  imem develop search "JWT" --decisions --limit 1
  imem develop search "" --file-path <result> --constraints
  imem develop search "" --session <result.session>

  **Future: Shortcut command (optional):**
  imem develop explain "JWT"  # Runs above sequence

  Same for trace/patterns.

  ---
  roadmap.md - Phase 7 section:

  OLD:
  Phase 7: Multi-Modal Retrieval
  - Implement explain() function
  - Implement trace() function
  - Implement patterns() function

  NEW:
  Phase 7: Multi-Modal Retrieval (DEFERRED)
  **Current approach:** Compose CLI primitives at runtime
  **Claude Code does:** `search + filter + filter` compositions
  **Slash commands:** Document common patterns

  **Future (optional):** Convenience wrappers
  - imem develop explain "topic"  # Runs 3 commands
  - imem develop trace "topic"    # Runs 5 commands
  - Only if composition patterns proven via usage

  ---
  roadmap.md - Phase 6 section:

  OLD:
  Phase 6: Soft-Graph Object Model
  - KnowledgeGraph class
  - Item/Pattern/Discussion objects
  - Lazy-loaded relationships

  NEW:
  Phase 6: Advanced Composition (DEFERRED)
  **Current approach:** CLI + JSON batch operations
  **Claude Code composes:** Parallel searches, graph construction

  **Future (optional):** Python API
  - KnowledgeGraph class (wraps CLI)
  - Item objects (lazy metadata queries)
  - Only if needed for programmatic access

  ---
  vision.md - No changes needed:
  - Concepts still accurate
  - Multi-modal retrieval = composition pattern
  - Soft-graph = metadata query model
  - Just clarify: implementation = CLI composition, not functions

  ---
  Updated Immediate Path

  Phase 5A+B: ✅ Complete (LlamaIndex chunking)

  Phase 6 (NEXT):
  - JSON batch operation support
  - Graph build/export/apply primitives
  - Combine operation for parallel results
  - Documentation slash command

  Phase 7 (DEFERRED):
  - Observe composition patterns first
  - Generate shortcuts only if proven
  - Python API only if programmatic use emerges

  Phase 8: Template validation (unchanged)

  Phase 9: Supersession detection (unchanged)

  ---
  Bottom Line

  Docs say: Build wrappers (explain/trace/patterns functions)Reality says: Compose
  primitives (CLI + flags at runtime)

  Edit approach:
  - Mark explain/trace/patterns as "future optional shortcuts"
  - Emphasize CLI composition as primary interface
  - Defer object model until proven need
  - Keep vision concepts (still valid)

  Result: Docs match reality = no confusion about what to build next.