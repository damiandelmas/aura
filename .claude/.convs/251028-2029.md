● Quickest Wins (Priority Order)

  1. Validate Conversation Metadata (~10 min) ⚡

  Why first: Verify recent work landed correctly before building on it.

  # Check if metadata is in Qdrant
  from qdrant_client import QdrantClient
  client = QdrantClient(host="localhost", port=6334)

  points, _ = client.scroll(
      collection_name="institutional_memory",
      scroll_filter={"must": [
          {"key": "source", "match": {"value": "conversation"}},
          {"key": "chunk_type", "match": {"value": "message"}}
      ]},
      limit=5, with_payload=True
  )

  # Test new filters
  imem conversations search "authentication" --patches-only
  imem conversations search "bug" --user-only

  Value: Confirms foundation is solid for Phase 6.

  ---
  2. CLI Symmetry Refactor (~1 hour) ⚡⚡

  Why second: Cleans interface BEFORE adding Phase 6 primitives.

  Changes:
  # Add: imem develop index
  # Add: imem document index
  # Move: imem index-conversation → imem conversations index
  # Move: imem index-all-conversations → imem conversations index-all
  # Extract: _index_phase(phase_name, force) helper

  After:
  imem develop index              # Symmetric with develop search
  imem conversations index <id>   # Under conversations group
  imem init --force               # Convenience (runs all phases)

  Value:
  - Clean mental model (phase.index, phase.search)
  - Easier to add Phase 6 commands (siblings/filter/graph)
  - Professional UX

  Risk: Low (just moving commands, logic unchanged)

  ---
  3. Siblings Primitive (~2-3 hours) ⚡⚡⚡

  Why third: Validated need (13+ uses), building block for Phase 6.

  Implementation:
  # imem/src/imem/relationships.py
  def get_siblings(chunk_id: str) -> List[Chunk]:
      """Get all chunks from same file_path"""
      chunk = get_chunk(chunk_id)
      return client.scroll(
          collection_name=collection,
          scroll_filter=Filter(must=[
              FieldCondition(key="file_path",
  match=MatchValue(value=chunk.payload['file_path']))
          ])
      )

  # CLI: imem siblings <result-id>

  Value:
  - Enables context assembly (decision + constraints + patterns)
  - Foundation for slash commands (/explain-decision)
  - Simple implementation (~60 lines)

  Unlocks: Phase 6 rest (filter, combine) and slash command library

  ---
  4. Template Validation (~2-3 hours) ⚡⚡

  Why fourth: Protects the moat (guaranteed metadata).

  Implementation:
  # imem/src/imem/validation.py
  def validate_changelog(content: str, section_type: str):
      """Enforce template compliance"""
      if section_type == 'Decisions':
          required = ['**Context**:', '**Solution**:', '**Rationale**:']
          for field in required:
              if field not in content:
                  raise ValidationError(f"Missing required field: {field}")
      return True

  # Add --strict flag to imem init (reject violations)

  Value:
  - Guarantees deterministic metadata (our unique differentiator)
  - Prevents template violations from degrading system
  - ~80 lines, high strategic value

  ---
  Execution Plan

  Session 1 (1.5 hours):
  1. Validate conversation metadata (10 min)
  2. CLI symmetry refactor (1 hour)
  3. Test refactor (20 min)

  Session 2 (2-3 hours):
  4. Siblings primitive implementation (2 hours)
  5. Test with real queries (30 min)
  6. Document usage pattern (30 min)

  Session 3 (2-3 hours):
  7. Template validation (2 hours)
  8. Test strict mode (30 min)
  9. Update docs (30 min)

  Total: ~6-8 hours for 4 high-value features

  ---
  Why These Wins?

  CLI refactor:
  - ✅ Low risk (no logic changes)
  - ✅ High UX value (clean interface)
  - ✅ Enables Phase 6 (clean place for siblings/filter/graph)

  Siblings primitive:
  - ✅ Validated need (13+ real uses documented)
  - ✅ Simple implementation (one Qdrant query)
  - ✅ Unlocks slash commands (context assembly)

  Template validation:
  - ✅ Protects moat (guaranteed metadata = our differentiator)
  - ✅ Prevents system degradation (violations caught early)
  - ✅ Strategic value (enables everything else)

  After these: Phase 6.5 (batch), Phase 7 (graph), Phase 8.5 (slash commands)