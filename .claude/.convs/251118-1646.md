  The Core Insight:

  We're building wrappers for queries we could just... write.

  # Instead of:
  results = store.get_same_document(chunk_id, limit=10)

  # Just do:
  results = db.execute('''
      SELECT * FROM chunks 
      WHERE file_path = (SELECT file_path FROM chunks WHERE id = ?)
      AND id != ?
      LIMIT 10
  ''', (chunk_id, chunk_id)).fetchall()

  ---
  What We Should Actually Do:

  Phase 4: Just Fix the False Abstraction (~1h)

  1. Split Protocols (30min)
  - VectorSearch (search, upsert) - Qdrant + SQLite
  - Remove graph methods from VectorSearch protocol
  - Remove fake implementations from Qdrant

  2. Document Query Patterns (30min)
  - Add SQL examples to docs showing how to query relationships
  - Don't wrap them in methods yet

  That's it.

  ---
  Why This Is Better:

  Current approach (premature):
  class GraphStore(Protocol):
      def get_same_document(...): ...
      def get_conversation_flow(...): ...
      def get_origin_conversation(...): ...
      def get_temporal_window(...): ...
      # 4 methods × 3 backends × testing = lots of code

  Better approach (just use SQLite):
  # In your retrieval code:
  same_doc = db.execute('''
      SELECT * FROM chunks WHERE file_path = ? AND id != ?
  ''', (file_path, chunk_id)).fetchall()

  conversation = db.execute('''
      SELECT * FROM chunks 
      WHERE session_id = ? AND source_type = 'conversation'
      ORDER BY timestamp
  ''', (session_id,)).fetchall()

  When to wrap: When you have 2-3 examples of the same pattern being used. Not before.

  ---
  Revised Phase 4: Minimal Protocol Fix (~1h)

  Goal: Make Qdrant honest about what it can do

  Changes:
  1. Keep VectorStore protocol as-is for SQLite (it works)
  2. Create VectorSearch-only interface for Qdrant
  3. Remove fake graph methods from Qdrant backend
  4. Update factory to return correct type

  Result:
  - SQLite: full VectorStore (search + graph queries work)
  - Qdrant: VectorSearch only (honest about capabilities)
  - Users query SQLite directly for graph ops (no premature abstraction)

  ---