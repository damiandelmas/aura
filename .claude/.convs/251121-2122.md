> **LlamaIndex** - MarkdownNodeParser for section extraction. Header hierarchy tracking.

are we stil using llmanaindex? 
  ⎿  ⧉ Selected 1 lines from ../../../.context/document/architecture.md in Visual Studio Code

● Bash(grep -n "llama_index\|LlamaIndex"
      /home/axp/projects/fleet/hangar/code/aura/worktrees/sql-first/imem/src/imem/compile/parser.py | head -5)
  ⎿  15:from llama_index.core.node_parser import MarkdownNodeParser  
     16:from llama_index.core.schema import Document as LlamaDocument
     124:    # Parse with LlamaIndex MarkdownNodeParser
     302:    # Parse with LlamaIndex

● Yes. compile/parser.py uses:
  - llama_index.core.node_parser.MarkdownNodeParser
  - llama_index.core.schema.Document

  Architecture doc is accurate on this point.

> why llamindex? didnt we make a custom parser? 

● You're right to question this. We had two parsers:

  1. compile/parser.py - Uses LlamaIndex MarkdownNodeParser (kept)
  2. parse/markdown.py - Custom regex-based parser (deleted as "orphaned")

  The custom one was orphaned/unused, so I deleted it during cleanup. But maybe that was the wrong call?

● Bash(git -C /home/axp/projects/fleet/hangar/code/aura/worktrees/sql-first/imem show HEAD:src/imem/parse/markdown.py 2>/dev/null |
      head -50 || echo "File not in git h…)
  ⎿  (No content)
  ⎿  API Error: 500 {"type":"error","error":{"type":"api_error","message":"Internal server error"},"request_id":null}

> why would you delete the custom one? the llmaindex one works for sqlite? 

● You're right. I made a bad call.

  What I did:
  - Saw parse/markdown.py wasn't imported anywhere
  - Deleted it as "orphaned"
  - Kept LlamaIndex dependency in compile/parser.py

  What I should have asked:
  - Why was there a custom parser?
  - Is LlamaIndex needed or just legacy baggage?
  - Which aligns with "Pure Python" mentioned in docs?

  LlamaIndex is a heavy dependency just for markdown section splitting. If there was a working custom parser, that was probably
  the intended direction.

  Can we recover it? Let me check if it's in git history or another branch:

● Bash(git -C /home/axp/projects/fleet/hangar/code/aura/worktrees/sql-first log --all --full-history --
      "imem/src/imem/parse/markdown.py" 2>/dev/null | head -10…)
  ⎿  commit 7433d035e1ee5d95ce8cbbe676e7a8f5960fadfb
     Author: damiandelmas <damiandelmas@gmail.com>
     Date:   Mon Nov 17 16:01:16 2025 -0800
     … +7 lines (ctrl+o to expand)