> Let's expand the tiers so we can be clearer.

Tier 0 = THE THING ITSELF; what the URL points to, what the file path points to
Tier 1 = objective metadata gateway
Tier 2 = subjective metadata gateway

Tier 2 is potential complex. It is the ENTRY POINT TO ANY 'THING ITSELF' for any use case. And could pass 
through several 'tier 2s?' lets not overengineering.

Let's just clear up this:   ## [Title]

  # OBJECTIVE (about the thing)
  **source:** [pointer - for Tier 2 fetch]
  **description:** [summary - for Tier 1 index]
  **created:** [valid time - when thing existed]
  **creator:** [attribution]

  # SUBJECTIVE (your relation to it)
  **added:** [transaction time - when you found it]
  **purpose:** [why you care]
  **project:** [barbar | npta | brain | global]
  **attention:** [computed from .brain/density]
  **status:** [active | archived | deprecated]

# IMPORTANT
(1) Objective would be a wrapper around the THING ITSELF that would point to it from any potential intention 
and WOULD NOT point it to any particular implemntaiton of it.

(A) THIS IS NOT sitting within every projects /.brain/ folder. This is an external repository of all knowledge 
sources, repos, etc. And each is exposed to the /.brain/ network by markdown files with 'OBJECTIVE' metadata.

(B) For instance — we can expose a repo that we want to study or utilize by simply dropping an entry into our 
OBJECTIVE REGISTRY that links the repo, has the OBJECTIVE METADATA.

(2) Subjective would be ANY GIVEN INTENTION or ENTRY POINT for a given use, project, purpose.

(A) we are in project-a and we retrieve url-x. at this point (a) a new intention is created; the project-a 
intention (b) there is now a project-a registry (c) we add a wrapper for url-x that sits within
project-a/.brain/ (using folder locations for purpose of articulative clarity). whenever we are in project-a
and we access something we thus go through its own gateway, and this accumulates intention, intertia. this
POINT becomes it's own 'brain' — ie, it becomes its own 'schema' or schema-intertia which is resolved at
runtime (and/or cached and utilized at runtime <<< everything doesnt 'need' to be ephemeral).

(B) In reference to (1B) — we RETAIN AND ACCUMULATE INTELLECTUAL CAPTIAL FOR EVERY SINGLE ENTRY when we 
reference it, refere to it, study it FROM AN INTENTION or from a project.

(C) This allows us to have a global registry of knowledge, repos, and in our use case (codebase design logs, 
develop logs etc) that retain a sense of ISOLATION, INDIVIUDALITY, OBJECTIVITY, but also ACCUMULATE subjective
INTELLECUTAL CAPTIAL FOR A GIVEN USE CASE.

In this sense, there is NOTHING different between a codebase and a develop log. or a URL pointing to anthropics
 official documentation and a design log ASIDE from that (a) it is wrapped by an OBJECTIVE GATEWAY WITH
OBJECTIVE DATA: the official doc is weighted differnetly wrt authroity, or it siply has an 'opffical tag' and
the design log is weigyted lower and has a 'speculative' or 'hypothses' tag etc etc. BUT BOTH point to
INTELLECTUAL CAPITAL. It is simply our WRAPPING that adjudicates its USE, and HOW WE SHOULD TREAT IT. Do we
treat it as hypothesis or tested? JUST WRAPPING IT WITH THAT intelligently unlocks the ability to utilize and
extract intelligence from all souruces.

Moreover, we never lose the referential and attentional energy that is used when we reference or research 
documentation FROM A PROJECT.

ultrathink.