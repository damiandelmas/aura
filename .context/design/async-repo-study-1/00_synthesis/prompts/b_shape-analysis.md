# Stage B: Shape Analysis

## Purpose

Compare and contrast architectural shapes across systems. Analyze data flow patterns (how information moves), separation of concerns (boundary definitions), dependency direction (what depends on what), and extension points (where flexibility lives). Synthesize the ideal structural patterns that enable our use case.
