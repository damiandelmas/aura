# Stage C: v1 Completion Plan

## Purpose

Concrete roadmap to working v1 using existing implementation plus minimal additions from Stage B. Specific files to create, functions to wrap, interfaces to expose. No architectural changes, no renaming, no refactoring—pure completion of current iteration. After this works and ships, then we consider structural improvements. Polish and finish what we have.
