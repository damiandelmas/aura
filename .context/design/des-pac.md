SQL DATABASE OVERARCHING ETHIC:

(1) Create endpoints that can be exploited by AI.

DO NOT wrap these to make it 'easier' for HUMAN.

The point is to expose endpoints that can be utilized by AI VIA inline json: imem compose {{inline json}}.

This enables us to build wrappers for endpoints without hardcoding 'ergonomics' that hamper flexibility. We want to discover our wrappers through use.

From use, we can extract out wrappers and concretize them into slash commands. This is the most agile method of creating modules and wrappers for endpoints; simply instruct the AI how to compose inline json to achieve a desired effect.

This enables us to discover wrappers, modules and presets. WE WILL NOT create a library of these WITHOUT first testing. AI is intelligent and can figure this out through use case.

(2) GRAPH OPERATIONS

AI can write PYTHON on the fly. This means we do not need to hardcode this either. We can enable the AI with a well-structured foundation of endpoints that can be utilized by ephemeral PYTHON.

(3) ENTITIES, RELATIONSHIPS etc

We can spawn 10 AI Brothers to maintain our entities, our metadata, etc.

This is 2025. We can allow a bit of flexibility here. In our plan, we should allow for this flexibility. We don't need to create weak, and unactualized 'stubs' that somewhat do what we want.