  Our conversation:
  - COMPILE (index-time metadata)
  - MANAGE (entity resolution, git validation)
  - EMERGENT (query-time assembly)

  This doc says:
  - L1: Implicit Graph (metadata predicates)
  - L2: Runtime Composition (NetworkX on k results)
  - L3: Contextual Assembly (traverse + template)
  - L4: Entity Resolution (variations → canonical)

  Different framing. Same concepts?

  | Our Layer | Their Layer                        |
  |-----------|------------------------------------|
  | COMPILE   | L1 (metadata at index)             |
  | MANAGE    | L4 (entity resolution)             |
  | EMERGENT  | L2 + L3 (runtime graph + assembly) |

  But "domains" in codebase = COMPILE/MANAGE/STORAGE/COMPOSE

  Are we conflating:
  - Conceptual layers (what happens when)
  - Code domains (where code lives)?

  Clarify?