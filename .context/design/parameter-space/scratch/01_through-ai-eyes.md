**Parameter Space Through AI-Generated Lens**

Now seeing your 35 parameters as **AI interpretations**, not ground truth:

**Template Metadata (AI's Structural Claims):**
- `section_type: "Decision"` = AI thinks this was a decision
- `has_rationale: true` = AI was confident enough to write reasoning
- `has_alternatives: true` = AI considered (or hallucinated) alternatives
- `word_count: 500` = AI was verbose (complex work OR overconfident)

**Document Properties (AI's Context Claims):**
- `category: "implementation.security"` = AI's interpretation of work type
- `temporal_position: "current_thrust"` = AI's guess about relevance
- `confidence.continuation_count: 3` = AI thinks this led to other work

**Discovery Enrichment (AI's Relationship Claims):**
- `siblings: [3 chunks]` = AI grouped these in same mental model
- `genealogy: [conversation]` = Traceable to actual human-AI discussion
- `temporal: [similar chunks]` = AI thinks these are related over time

**The Reliability Spectrum:**

**HIGH RELIABILITY (Hard Facts):**
- `timestamp` = When AI wrote this
- `session_id` = Which conversation produced this
- `file_path` = Where AI put this
- `source: "conversation"` = Traceable to human input

**MEDIUM RELIABILITY (AI Structure):**
- `section_type` = AI followed template (probably correct)
- `word_count` = Objective metric (but biased by AI verbosity)
- `has_context/solution` = AI included required fields

**LOW RELIABILITY (AI Interpretation):**
- `temporal_position` = AI's guess about supersession
- `has_rationale` = AI wrote something, quality unknown
- `authority_score` = Based on AI's relationship claims

**The Real Intelligence:**
Use high-reliability metadata to validate low-reliability claims. Cross-reference AI interpretations with conversation genealogy and temporal patterns to build actual confidence scores.