● Current Implementation Status

Migrated to Nomic ✅ with smart fallback:

What's Live Now

Default model: Nomic Embed v1.5 (768D, 8k tokens)

Auto-detection: Collections self-describe
- E5 collections → automatically loads E5 model
- Nomic collections → automatically loads Nomic model
- Fallback → uses default (Nomic) if detection fails

Committed: 3481866 - 4 files changed
- config.py: MODEL_REGISTRY + Nomic defaults
- enhanced.py: Auto-detect from collection vector config
- cli.py: --collection override flag
- ingest.py: Nomic loading + updated thresholds

Collection Status

Re-indexed with Nomic:
- ✅ Main project (aura/main) - imem_1ba1fff1_context

Still using E5 (auto-detect works):
- Sandbox projects (imem_2012b868, etc.)
- Other registered projects
- Will re-index when convenient

Test collection:
- Created: imem_77eb0e65_nomic (for A/B comparison)
- Can delete when testing complete

Smart Behavior

# When searching any collection:
collection = client.get_collection(name)
vector_name = collection.config.params.vectors.keys()[0]  # "e5-large-v2" or "nomic-embed-v1.5"
model_info = MODEL_REGISTRY[vector_name]  # Lookup correct model
encoder = SentenceTransformer(model_info["model_path"])  # Load it

No manual switching needed. Just works. ✅

---
TL;DR: Default is Nomic. Old E5 collections still work via smart fallback. Can A/B test with
--collection flag.