{
  "description": "Find documented failures, anti-patterns, and lessons learned",
  "use_case": "Learn from past mistakes - what was tried, why it failed, what not to suggest again",
  "parameters": {
    "topic": {
      "type": "string",
      "required": true,
      "description": "Failure area or approach to investigate"
    }
  },
  "compose_config": {
    "search": {
      "text": "{{topic}}",
      "filters": {"phase": "develop"},
      "limit": 5
    },
    "discovery": {
      "siblings": {
        "section_types": ["Failures", "Constraints", "Lessons"],
        "has_rationale": true,
        "limit": 15
      }
    }
  }
}
