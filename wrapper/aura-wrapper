#!/bin/bash
# aura-wrapper - Launch Claude through aura PTY wrapper
#
# Usage:
#   aura-wrapper              # Fresh session
#   aura-wrapper -r SESSION   # Resume session

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec python3 "$SCRIPT_DIR/aura.py" "$@"
